import 'cypress-iframe'

describe('IFrame Suite', () => {
  it('IFrame Test', () => {

    cy.visit('https://demoqa.com/frames')

  //  cy.frameLoaded('frame1'); //frame name

  //  cy.frameLoaded('sampleHeading'); //frame id

    // cy.iframe().contains('page').click();
  //  
  })
})